import glamorous from 'glamorous';

import v from '../../../styles/variables';

export const App = glamorous.div({
  color: v.colorFont,
  fontSize: v.fontSizeBase,
  fontWeight: v.fontWeightLight,
  letterSpacing: v.fontSpacingBase,
  textAlign: 'center'
});
